import { CompassHeadingPipe } from './compass-heading-pipe';

describe('CompassHeadingPipe', () => {
  it('create an instance', () => {
    const pipe = new CompassHeadingPipe();
    expect(pipe).toBeTruthy();
  });
});
